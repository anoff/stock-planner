{
  "name": "stock-planner-cors-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker CORS proxy for Yahoo Finance API requests",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250525.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.14.4"
  }
}
